{% block body %}
    <form id="form" action="{{ path('update_video', {'id':video.id}) }}" method="post" enctype="multipart/form-data">
        <div id="form-container">
            {{ form_row(form.url) }}
            {{ form_rest(form) }}
        </div>
        <div class="form-group text-center">
            <input type="submit" value="Modifier" class="btn btn-success"/>
        </div>
    </form>
{% endblock %}